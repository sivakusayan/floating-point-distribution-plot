Code behind [this blog post](https://sayansivakumaran.com/posts/2024/11/plotting-floating-point-distribution/).
To generate the graph, you need these dependencies:

- A C compiler
- gnuplot
- make

Once you have those, all you need to do is invoke `make` in the command line.
